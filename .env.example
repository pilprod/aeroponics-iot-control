# These example variables must be exported; the controllers do not load this file.
MQTT_BROKER=localhost
MQTT_USERNAME=
MQTT_PASSWORD=

# Each controller also supports MQTT_TOPIC_* overrides documented in its source.
# The default topics use a generic portfolio namespace, not a real device address.
